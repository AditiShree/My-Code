package main 

import (
	"fmt"

)

func main() {
	//p[] = {p0,p1,p2,p3,p4,p5}
	t := make([]string, 1)
	var p[6] string
	var arr[6], exec[6] int
	var init[6], comp[6], remain[6], wait[6], taTime[6] int
	var done[6] bool
	time := 0
	quan := 100

	for i:=0; i<6; i++ {
		done[i] = false
		fmt.Scan(&p[i], &arr[i], &exec[i])
	}

	t[0] = p[0]

	if i:=0; !done[5]; i++ {
		if exec[i] > quan {
			exec[i] = exec[i] - quan
			p[i] =   
		}
	}

}