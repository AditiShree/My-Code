#include <stdio.h>

struct node {
  int data;
  node* left;
  node *right;
};

main() {
  int n, d, i;
  printf("Enter tye no of elem\n" );
  scanf("%d", &n);

  node *head, *tmp;

  for(i=0; i<n; i++) {
    scanf("%d", &d);

    if(n==0) {
      head->data = d;
      head->
    }
  }



}
